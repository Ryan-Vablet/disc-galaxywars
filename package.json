{
  "name": "disc-galaxywars-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace root for Node Wars client and server",
  "workspaces": [
    "game/client",
    "game/server"
  ],
  "scripts": {
    "dev": "node scripts/dev-all.mjs",
    "dev:client": "npm run dev --workspace game/client",
    "dev:server": "npm run dev --workspace game/server",
    "build": "npm run build --workspace game/client && npm run build --workspace game/server",
    "deploy:gh-pages": "npm run build:pages --workspace game/client && npm run deploy:gh-pages --workspace game/client",
    "test": "npm run test --workspace game/client",
    "lint": "npm run lint --workspace game/client"
  }
}
